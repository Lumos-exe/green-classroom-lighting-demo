你现在要帮助我重构一个 Blender 教室动画场景。请不要只做一个粗糙示意模型，而是要根据我提供的教室参考照片和已知尺寸，尽可能真实地重建这个阶梯教室，并修复当前动画中“人物滑稽、穿模、教室结构失真、没有门、路径不合理”等问题。

【任务目标】
请基于参考照片，输出一个更真实、更稳定的 Blender 场景构建与动画方案，并优先生成可直接运行的 Blender Python 脚本；如果脚本较长，也可以分模块给出。重点是：
1. 教室结构要尽量贴近真实照片；
2. 人物不能再穿过桌椅或墙体；
3. 人物移动路径必须沿真实过道和台阶进行；
4. 场景必须包含门、窗、黑板、讲台、屏幕、灯具、投影等关键元素；
5. 整体视觉要从“粗糙演示”提升到“结构真实、细节可信、适合展示”。

【已知尺寸与建模基准】
请以米为单位建模，并把所有关键尺寸写成可调参数。
已知信息如下：
- 教室高度至少 3.5m，建议默认层高设置为 3.6m～3.8m；
- 地砖边长约 0.6m（60cm×60cm），可作为场景比例标尺；
- 右侧长墙长度约 15.67m；
- 前方黑板墙横向宽度约 10.03m；
- 某窗墙竖向量测约 3.2m，可作为窗区高度参考；
- 天花灯具一行 4 个，整体按规则网格排布；
- 这是一个阶梯教室，不是平地教室，桌椅位于逐级升高的平台上。

【参考照片使用要求】
我会提供多张照片，请你全部使用，不要只凭其中一张图建模。
请综合不同视角，提取以下信息：
- 整体空间比例；
- 讲台和黑板区域；
- 门的位置；
- 窗户、窗帘、显示屏的位置；
- 座椅排布；
- 中间/侧边过道和台阶；
- 天花板灯具、出风口、投影设备、梁；
- 后墙材质和音响；
- 地面材质和地砖网格。

如果某些局部尺寸不确定，请根据照片、地砖尺寸和整体比例进行合理估计，但必须把估计值做成参数，便于后续调整。

【场景结构要求】
请按真实教室而不是抽象教室建模。至少包含以下部分：

1. 教室主体
- 正确建立四面墙、地面、天花板；
- 右侧是一整面较长的白墙；
- 左侧有多扇高窗，黑色窗框，配灰色窗帘；
- 后墙有浅色/米色吸音板质感，并有黑色音响；
- 顶部为吊顶天花，带规则分块；
- 天花上有多排嵌入式灯具、若干空调/通风口，以及横向/纵向梁结构；
- 存在吊装投影设备/摄像设备，请按照片位置还原，不要省略。

2. 前方教学区
- 前墙包含黑板系统，不是简单的一块单独黑板；
- 需要按照片做出多块拼接/滑动式黑板区域；
- 前方中央有白色讲台；
- 前方靠左有大尺寸电子显示屏/电视，立在支架上；
- 前方靠右有一扇门；
- 前墙右侧还有时钟、告示/海报、配电箱等小元素；
- 前右角有两个垃圾桶（黑色和蓝色）。

3. 座椅与台阶
- 这是固定连排桌椅，不是独立课桌；
- 座椅材质为浅木色板面 + 浅灰色金属支架；
- 行列排布要根据照片还原，不要随便平均排；
- 场地是阶梯式抬升的，必须做出台阶结构；
- 桌椅每排之间高度逐渐抬高；
- 中间/局部区域有台阶过道；
- 座椅下方有金属置物篮等细节可以适度简化但不要完全省略；
- 桌椅支架、靠背、桌板比例要尽量接近参考照片。

4. 窗户与侧面细节
- 左侧多扇高窗，窗框为深色；
- 窗外可简单做树木/绿化背景平面，不需要高精度，但要有透窗效果；
- 每个窗位配灰色落地式窗帘；
- 局部有栏杆/平台边缘结构，请结合照片决定是否保留。

【动画修复要求】
当前版本的问题是人物动作粗糙、穿模、移动逻辑不真实。请重点修复：

1. 禁止穿模
- 不允许人物穿过桌椅、讲台、墙体；
- 需要为桌椅区、讲台、墙体建立碰撞逻辑或等效的路径限制逻辑；
- 如果用简化碰撞，请至少用包围盒/导航区域避免穿模。

2. 路径规划
- 人物只能沿可通行区域移动：门口/入口 → 过道 → 台阶过道 → 对应座位；
- 不允许直接直线穿越桌椅区；
- 请给出 walkable area / navigation path 的实现思路；
- 若使用脚本，请将可通行路径点或导航节点参数化。

3. 入座逻辑
- 人物到达某一排后，应在排入口处转向，再沿该排进入座位；
- 到达座位后再执行坐下动作；
- 不要让人物直接“瞬移”到座位中间。

4. 动作与节奏
- 当前动画过于滑稽，请改善时间曲线；
- 位置与旋转插值应使用缓动（ease in/out），避免机械直线感；
- 多个人物进入时应错峰，不要完全同步；
- 行走速度、转身、停顿、坐下应更自然。

5. 镜头与表现
- 可保留一个主镜头，也可增加少量切换镜头；
- 建议至少有：
  a. 教室全景镜头；
  b. 面向讲台的镜头；
  c. 侧面体现阶梯结构的镜头；
- 画面帧率建议 24fps 或 30fps，不要再使用低帧率导致卡顿感。

【材质与渲染要求】
不要求电影级写实，但必须明显优于“纯几何示意”：
- 地面：灰色瓷砖，带 60cm 网格感；
- 墙面：白墙 + 后墙浅米色吸音板；
- 桌椅：浅木色板材 + 浅灰色金属；
- 窗框：深色；
- 窗帘：灰色；
- 黑板：深绿色/黑色板面；
- 讲台：白色；
- 灯光：模拟室内均匀顶灯照明，可叠加少量窗外自然光；
- 渲染目标：结构清楚、材质明确、阴影自然，不追求极端写实，但不能廉价。

【实现方式要求】
请优先输出以下内容：
1. 一个可直接运行或接近可运行的 Blender Python 脚本；
2. 脚本中将关键几何尺寸写成参数，例如：
   - room_length
   - room_width
   - room_height
   - tile_size
   - window_width
   - window_height
   - desk_row_count
   - desk_col_group_layout
   - step_height
   - aisle_width
   - front_platform_depth
   等；
3. 把“根据照片估计”的尺寸和“已知尺寸”区分开；
4. 在脚本中清楚分模块：
   - 清空场景
   - 创建教室主体
   - 创建窗户/门
   - 创建讲台/黑板/显示屏
   - 创建阶梯与桌椅
   - 创建灯具/投影/音响
   - 创建角色路径点
   - 创建人物动画
   - 设置相机与灯光
5. 如果某部分无法一次性精确实现，也请先给出结构合理、可继续迭代的版本，而不是退回到粗糙示意模型。

【特别注意】
- 不要偷懒把教室简化成一个长方体房间加几排方块桌椅；
- 不要忽略门；
- 不要忽略阶梯台阶；
- 不要忽略显示屏、窗户、黑板系统、投影设备；
- 不要让人物走直线穿过桌椅；
- 不要只给思路，请尽量给出实做代码；
- 如果照片与尺寸存在局部矛盾，请优先保持整体视觉比例正确，并把不确定项参数化。

【最终输出形式】
请按以下顺序输出：
1. 你从参考图中识别到的教室结构总结；
2. 你采用的主要尺寸假设；
3. 场景重建方案；
4. 动画修复方案；
5. Blender Python 脚本（分段给出，必要时分多次输出）。